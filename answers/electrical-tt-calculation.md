# Отчёт о фактическом электротехническом расчёте (саморегулирующийся кабель ТТН/ТТВ/ТТХ)

**Дата фиксации:** 2026-08-16 (первая фиксация 2026-08-15)
**Проверено по коду:** формульный контракт `electrical-tt-v3-case1-r6` (DEC-19)
**Статус:** снимок фактического поведения; не нормативная документация
**Объект расчёта:** электрорасчёт `self_regulating_tt`, контракт `electrical-tt-v3-case1-r6`
**Первоисточник методики:** Кейс ТНП, ред. 4 от 07.07.2026, §6.12–6.14 и §6.16
**Аудитория:** продакт-оунер; описано текущее поведение («как есть»), не целевая методика

Отчёт прослеживает путь данных от полей формы и результатов теплорасчёта до
сохранённого электрического результата: источники и приоритеты входов,
каталоги, формулы, порядок вычислений, блокирующие проверки, состав
результата. Легаси-линейка условных марок «ТЛТ» удалена (DEC-07);
рассчитывается только саморегулирующийся кабель серий ТТН/ТТВ/ТТХ.

Путь данных:

    Тепловой объект (params/results)   q, L, T, Kзап        calculation_service._tt_object_heat_inputs
      + запрос ЭР (навив, нитки, марка)                      cable-selection / batch API
      + assignment-переопределения ЭР                        electrical_assignment_service
      + настройки проекта (U, Iдоп)                          project_electrical_settings
      → канонический резолвер входов    приоритет источников electrical_input_resolver.py
      → каталоги power/section/BOM      неизменяемые снимки  electrical_catalog_service
      → подбор марки и ниток            §6.13                self_regulating.py
      → секционирование                 §6.14                sections.py
      → финальный гейт                  §6.14                tt_final_gate.py
      → результат ЭР                    fingerprint, статусы electrical_calculations

Сквозной пример (полный разбор — раздел 15): труба с базовыми теплопотерями
25 Вт/м, Kзап = 1,2 → 1,1 (тепловой), длиной 50 м, прямая укладка,
T объекта 40 °C, T среды −30 °C, T вкл −20 °C. Автоподбор выбирает
`30ТТВ2-СР`, 1 нитку; секционирование даёт 1 секцию длиной 95 м
(Lогр = 95 м), фактическую длину 95 м, заказ 104,5 м, мощность 2850 Вт,
стартовый ток 18,525 А, рабочий ток 12,391 А.

---

## 1. Общая модель расчёта

Электрорасчёт не содержит собственной теплофизики. Он принимает готовые
теплопотери из теплорасчёта и решает подборочную задачу:

1. привести требуемую мощность к Вт на метр укладки кабеля и умножить на
   запас: \(P_{\text{треб}} = q \cdot K\);
2. выбрать из каталога минимально достаточную марку и число ниток так, чтобы
   \(P_{\text{каб}} \cdot K_{\text{нав}} \cdot N \ge P_{\text{треб}}\)
   при соблюдении температурных ограничений марки;
3. рассчитать требуемую длину кабеля
   \(L_{\text{треб}} = L_{\text{базы}} \cdot K_{\text{нав}} \cdot N\);
4. разбить длину на одинаковые нагревательные секции по паспортному
   ограничению \(L_{\text{макс}}\) и токовому ограничению \(L_{\text{ток}}\);
5. посчитать мощности и токи от фактической длины секций;
6. пропустить результат через финальный гейт §6.14 и сохранить его вместе со
   снимками каталогов и фингерпринтом формулы.

### 1.1. Названия шагов и их характер

| Шаг | Характер правила | Фактическая запись |
|---|---|---|
| Требуемая мощность | Бизнес-правило §6.13; запас применяется ровно один раз | `P_req = q * K` |
| Коэффициент навива | Геометрия винтовой линии + нормативная таблица максимумов | `K_nav = sqrt(1 + (pi*D/S)^2)` |
| Пригодность марки | Каталожная проверка §6.13 | `T_env >= T_min`, `T_product <= T_max` |
| Покрытие мощности | Каталожная проверка §6.13 | `P_cable * K_nav * N >= P_req` |
| Выбор лучшего | Лексикографическая сортировка §6.13 | `(N, P_cable, P_cable*N, марка)` |
| Длина по току | §6.14 | `L_ток = Iдоп / Iст.уд` |
| Допустимая длина секции | §6.14, округление вниз | `L_огр = min(L_макс, L_ток)` |
| Число секций | §6.14, округление вверх | `n = ceil(L_треб / L_огр)` |
| Одинаковые секции | §6.14: все секции равной длины | `L_сек = L_огр`, `L_факт = L_сек * n` |
| Токи и мощность | §6.14, от фактической длины | `I = P_кабеля_на_метр * L / U` |
| Заказная длина | Монтажный запас 10 %, округление вверх до 0,001 м | `L_заказ = ceil(L_факт * 1.10)` |

Ни одна из формул не является физическим законом; это принятая
методика источника (Кейс ТНП §6.13–6.14) и паспортные данные каталогов.

---

## 2. Обозначения

| Обозначение | Величина | Единица |
|---|---|---:|
| \(q\) | требуемая мощность обогрева на метр укладки до запаса | Вт/м |
| \(K\) | коэффициент запаса электрорасчёта | безразмерный |
| \(P_{\text{треб}}\) | требуемая мощность с запасом | Вт/м |
| \(P_{\text{каб}}\) | паспортная мощность марки (`nominal_power`) | Вт/м |
| \(P_{\text{уст}}\) | установленная мощность на метр объекта | Вт/м |
| \(L_{\text{базы}}\) | базовая длина укладки без заказного запаса | м |
| \(D\) | наружный диаметр трубы | мм |
| \(S\) | шаг навива | мм |
| \(K_{\text{нав}}\) | коэффициент навива | безразмерный |
| \(N\) | количество ниток кабеля | шт. |
| \(T_{\text{прод}}\) | температура продукта (требуемая температура объекта) | °C |
| \(T_{\text{среды}}\) | расчётная температура окружающей среды | °C |
| \(T_{\text{вкл}}\) | минимальная температура включения объекта | °C |
| \(T_{\min}, T_{\max}\) | температурные ограничения марки | °C |
| \(U\) | рабочее напряжение | В |
| \(I_{\text{доп}}\) | максимально допустимый стартовый ток одной секции | А |
| \(I_{\text{ст.уд}}\) | удельный стартовый ток кабеля | А/м |
| \(L_{\text{макс}}\) | паспортная максимальная длина секции | м |
| \(L_{\text{ток}}\) | максимальная длина секции по стартовому току | м |
| \(L_{\text{огр}}\) | допустимая длина одной секции | м |
| \(L_{\text{треб}}\) | требуемая общая длина кабеля | м |
| \(L_{\text{сек}}\) | длина одной секции | м |
| \(L_{\text{факт}}\) | фактическая общая длина секций | м |
| \(L_{\text{доп}}\) | дополнительная длина сверх требуемой | м |
| \(L_{\text{заказ}}\) | заказная длина с монтажным запасом | м |
| \(n\) | количество нагревательных секций | шт. |

---

## 3. Исходные данные и их источники

### 3.1. Приоритет источников (резолвер)

Каждое каноническое поле разрешается один раз перед формулами в строгом
порядке приоритета (`electrical_input_resolver.py`):

1. `explicit_request` — значение из текущего запроса (cable-selection,
   batch);
2. `assignment_override` — сохранённое переопределение объекта в конкретном
   электрорасчёте (ЭР);
3. `project_setting` — настройки проекта (только \(U\) и \(I_{\text{доп}}\));
4. `object_heat` — параметры и результаты теплового объекта;
5. mock-профиль (только режимы dev/test; помечает расчёт
   `production_eligible=false`);
6. `not_set` — допустимое «пусто» для трёх полей: шаг навива (прямая
   укладка), нитки (автоподбор), марка (автоподбор).

Поле \(I_{\text{доп}}\) особое: либо `project_setting`, либо
`section_catalog_derived` (вывод из каталога, раздел 10.1); из запроса и
объекта оно не принимается. Источник каждого поля сохраняется в
`input_sources`.

### 3.2. Поля тепловой карточки объекта, которые читает электрорасчёт

| Лейбл формы | Поле объекта | Каноническое поле | Диапазон формы | Как участвует |
|---|---|---|---|---|
| Требуемая температура объекта | `process_temperature` | `product_temperature_c` | −90…600 °C | Проверка \(T_{\text{прод}} \le T_{\max}\) марки |
| Температура окружающей среды | `ambient_temperature` | `ambient_temperature_c` | −70…70 °C | Проверка \(T_{\text{среды}} \ge T_{\min}\) марки; см. 3.3 |
| Температура грунта | `ground_temperature` | участвует в `ambient_temperature_c` | −70…70 °C | Для подземных объектов, см. 3.3 |
| Минимальная температура включения | `min_switch_temperature` | `cold_start_temperature_c` | **−40…10 °C**, шаг 0,1 | Выбор строки каталога секционирования |
| Коэффициент запаса (Kзап) | `safety_factor` / `results.safety_factor_applied` | `safety_factor` | 1,05–1,7 (труба от 1) | \(P_{\text{треб}} = q\cdot K\) |
| Длина трубопровода | `pipe_length` → `results.effective_length` | `base_length_m` | 0,5–200 000 м | База длины кабеля; см. 4.1 |
| Наружный диаметр | `outer_diameter` (м) ×1000 | `outer_diameter_mm` | 10,8–3000 мм | Проверка шага навива и максимума \(K_{\text{нав}}\) |
| Высота обогрева резервуара | `heating_height` | геометрия укладки | от 0,001 м | Длина укладки на резервуар |
| Шаг укладки резервуара | `laying_step` | геометрия укладки | 0,1–0,4 м | Длина укладки на резервуар |
| Количество ниток кабеля | `number_of_threads` | `thread_count` | 1–3 | Фиксация числа ниток |
| — (сохранённый) | `winding_pitch` | `winding_pitch_mm` | см. 3.4 | Коэффициент навива |

Дефолт формы для «Минимальная температура включения» — −20 °C.
Диапазон −40…10 °C в точности совпадает с сеткой температур каталога
секционирования.

### 3.3. Расчётная температура среды

Правило `_tt_ambient_temperature` выбирает одно значение \(T_{\text{среды}}\)
из пары «температура воздуха / температура грунта» объекта:

| Условие | Фактическое значение |
|---|---|
| Размещение не «Подземно» | `ambient_temperature`, при его отсутствии `ground_temperature` |
| Подземная труба | `ground_temperature`, при его отсутствии `ambient_temperature` |
| Подземный резервуар | минимум из имеющихся `ambient_temperature` и `ground_temperature` |

Значение используется только в температурной проверке марки (раздел 7)
и в списке марок для ручного выбора. В расчёт длин, токов и мощностей
температура среды не входит.

### 3.4. Поля рабочего пространства ЭР

| Лейбл UI | Поле запроса | Канон | Ограничения UI | Куда идёт |
|---|---|---|---|---|
| Шаг навива, мм (ячейка таблицы) | `winding_pitch_mm` | `winding_pitch_mm` | ≥ 0; строго больше D мм; итоговый \(K_{\text{нав}}\) ≤ максимума диапазона D; для резервуаров редактирование запрещено | \(K_{\text{нав}}\) |
| Ниток (ячейка таблицы) | `thread_count` | `thread_count` | целое 1–3 | Фиксация N |
| Модальное окно «Выбор марки кабеля» → Марка | `cable_mark` (+`mode: auto/manual`) | `manual_cable_model` | точный full_mark из списка | Ручной режим подбора |
| Модальное окно → Количество ниток | `thread_count` | `thread_count` | `auto` либо 1/2/3 | Фиксация N |
| Перезаписать ручные выборы | `skip_manual` (инверсия) | — | чекбокс | Пакетный пересчёт |

Контрол «Критерий подбора» существует в коде UI, но скрыт флагом
(`SHOW_COMMERCIAL_CABLE_BASE_UI = false`); фактически всегда передаётся и
принимается только `selection_policy = technical_minimum` — любое иное
значение отклоняется и резолвером, и формулой.

Значение шага навива 0 равнозначно прямой укладке (\(K_{\text{нав}} = 1\)).

### 3.5. Настройки проекта

| Лейбл UI | Поле | Ограничения | Как участвует |
|---|---|---|---|
| Режим I доп проекта: «Автоматически по каталогу» / «Вручную» | `max_section_start_current_a` (null = авто) | > 0 | Токовое ограничение длины секции |
| — (нет UI) | `nominal_voltage_v` | CHECK-ограничение БД: ровно 230 | Расчёт токов |

Напряжение недоступно для редактирования нигде: фронтенд жёстко отправляет
`supply_voltage = 230` в пакетных запросах, PATCH настроек проекта поле не
принимает, БД ограничена значением 230.

### 3.6. Поля, исключённые из контракта TT

Запрос с любым из этих полей отклоняется с кодом `ELECTRICAL_INPUT_RETIRED`:
`max_section_start_current_a` (в запросе), `maintain_temperature`,
`vapor_temperature`, `steam_tracing`, `aggressive_product`,
`winding_coefficient`, `connection_type` и их алиасы.

При этом сама форма объекта по-прежнему собирает и хранит
«Температура поддержания», «Пропарка», «Температура пропарки», «Среда»,
«Температурная группа» — TT-расчёт эти параметры объекта не читает.
Единственная температура продукта в расчёте — «Требуемая температура
объекта» (`process_temperature`).

---

## 4. Производные входы из теплорасчёта

Расчёт доступен только для объекта с актуальными (не stale) тепловыми
результатами; иначе `ELECTRICAL_HEAT_LOSS_REQUIRED`.

### 4.1. Труба

**Смысл действия:** взять из теплового результата базовые (до запаса)
линейные теплопотери и эффективную длину.

\[
q = \text{heat\_loss\_per\_meter\_base}
\qquad
L_{\text{базы}} = \text{effective\_length} = L + n_{\text{лок}} L_{\text{экв}}
\]

Резерв: при отсутствии `effective_length` берётся `pipe_length`. Локальные
элементы теплорасчёта тем самым увеличивают и длину закупаемого кабеля.

### 4.2. Резервуар

**Смысл действия:** снять тепловой запас с проектной суммарной теплопотери
(она уже содержит \(K_{\text{тепл}}\)), распределить её по длине укладки и
позволить электрической формуле применить запас заново — итог равен исходной
проектной теплопотере при совпадении запасов.

Длина укладки по поверхности:

\[
L_{\text{базы}}
=
\frac{\text{периметр}}{2}\cdot
\frac{h_{\text{обогр}}}{w_{\text{шаг}}}
\qquad
\text{периметр}=
\begin{cases}
\pi D_{\text{рез}}, & \text{цилиндр}\\
2(l+w), & \text{прямоугольный}
\end{cases}
\]

\[
q =
\frac{\text{total\_heat\_loss\_design} / K_{\text{тепл}}}{L_{\text{базы}}}
\]

Тепловой результат резервуара имеет вид
\(Q_{\text{расч}} = q_{\text{уд}} S K_{\text{тепл}} + Q_{\text{доп}}\), поэтому
деление на \(K_{\text{тепл}}\) и последующее умножение электрорасчёта на тот
же \(K\) восстанавливают \(Q_{\text{расч}}\) точно, включая долю
\(Q_{\text{доп}}\). Бизнес-правило §6.13: «запас применяется один раз».

Для резервуара любые трубные входы навива (`winding_pitch`,
`outer_diameter_mm`) в запросе, параметрах объекта или переопределениях ЭР
блокируются кодом `ELECTRICAL_TANK_LAYOUT_INPUT_UNSUPPORTED`;
\(K_{\text{нав}} = 1\). Допустимые формы — только `cylindrical` и
`rectangular`.

### 4.3. Требуемая мощность

\[
P_{\text{треб}} = q \cdot K
\]

\(K\) берётся из `results.safety_factor_applied` теплорасчёта (резерв —
`params.safety_factor`) и может быть переопределён на уровне запроса или
assignment.

---

## 5. Каталоги

Расчёт использует три снимка каталогов; их версии и контрольные суммы
включаются в результат и в фингерпринт расчёта.

### 5.1. Power-каталог (паспортные мощности)

14 базовых моделей; `nominal_power` — паспортная мощность \(P_{\text{каб}}\),
`max_product_temp` — \(T_{\max}\):

| Серия | Модели (P, Вт/м) | \(T_{\max}\), °C |
|---|---|---:|
| ТТН | 10ТТН2 (10), 17ТТН2 (17), 25ТТН2 (25), 31ТТН2 (31) | 65 |
| ТТВ | 15ТТВ2 (15), 30ТТВ2 (30), 45ТТВ2 (45), 60ТТВ2 (60) | 120 |
| ТТХ | 15ТТХ2 (15), 30ТТХ2 (30), 45ТТХ2 (45), 60ТТХ2 (60), 75ТТХ2 (75), 90ТТХ2 (90) | 150 |

Температурные коэффициенты мощности `q1`, `q2` в строках присутствуют, но
текущим расчётом не используются: мощность берётся паспортной константой
(легаси-формула \(P(T_3)\) удалена вместе с линейкой «ТЛТ»).

Поля `max_vapor_temp` и `voltage` каталога в расчёте также не участвуют.

### 5.2. Каталог секционирования («Параметры Кабеля.xlsx», §6.14)

126 строк: 14 моделей × 9 температур включения (+10…−40 °C с шагом 5 до
−20, далее −30, −40). Строка: `марка × Тмин_вкл × Lмакс × Iст.уд`.
Первоисточник не содержит \(I_{\text{доп}}\) — все строки имеют
`i_dop_a = null`.

Правило выбора строки: точная модель (суффиксы «-СТ»/«-СР» полной марки
отбрасываются) и **ближайшая более низкая либо равная** температура
включения; более тёплая строка не подставляется никогда. Пример из §6.14:
объект −17 °C, строки −10 и −20 → используется −20. Если T вкл ниже самой
холодной строки — `ELECTRICAL_SECTION_CATALOG_ROW_NOT_FOUND`.

Из этого же каталога выводится \(T_{\min}\) марки: минимум температур
включения по всем строкам модели (сейчас −40 °C для всех 14 моделей).

### 5.3. BOM-каталог (номенклатура)

18 позиций полных марок: для ТТН существуют исполнения «-СТ» и «-СР»
(8 позиций), для ТТВ и ТТХ — только «-СР» (10 позиций). Каждая позиция несёт
`nomenclature_code`. Кандидат подбора — каждая полная марка BOM, чей префикс
до дефиса совпадает с базовой моделью power-каталога; отсутствие BOM-позиции
для выбранной марки блокирует расчёт (`SPEC_CABLE_NOMENCLATURE_MISSING`).

### 5.4. Авторитет каталогов

Для production-расчёта все три каталога должны быть активными
зарегистрированными версиями с контрольной суммой; иначе расчёт блокируется
(`ELECTRICAL_CATALOG_SOURCE_UNREGISTERED`) либо помечается предупреждением
`ELECTRICAL_POWER_CATALOG_PROVISIONAL` и `production_eligible=false`.

---

## 6. Коэффициент навива

**Тип формулы:** длина винтовой линии на цилиндре (геометрия) + нормативная
таблица максимумов из §6.13.

Прямая укладка (шаг не задан или 0):

\[
K_{\text{нав}} = 1
\]

При заданном шаге \(S\) (мм) и диаметре \(D\) (мм):

\[
S > D
\quad\text{иначе отказ}
\qquad
K_{\text{нав}} = \sqrt{1+\left(\frac{\pi D}{S}\right)^{2}}
\]

Максимум по диаметру:

| Диаметр, мм | \(K_{\text{нав}}^{\max}\) |
|---|---:|
| < 57 | 1,0 |
| = 57 | 1,1 |
| 57 < D ≤ 75 | 1,2 |
| 75 < D ≤ 89 | 1,3 |
| 89 < D ≤ 108 | 1,4 |
| > 108 | 1,5 |

Превышение — отказ `ELECTRICAL_WINDING_FACTOR_LIMIT_EXCEEDED`. Следствие
таблицы: для труб диаметром меньше 57 мм любой навив недопустим
(\(K_{\text{нав}}>1{,}0\) при любом \(S>D\)). Для резервуаров всегда
\(K_{\text{нав}}=1\).

Результат округляется до 6 знаков.

---

## 7. Температурная пригодность марки

**Тип правила:** каталожная проверка §6.13.

Марка пригодна, если одновременно:

\[
T_{\text{среды}} \ge T_{\min}
\qquad\text{и}\qquad
T_{\text{прод}} \le T_{\max}
\]

где \(T_{\max}\) — `max_product_temp` power-каталога (65/120/150 °C), а
\(T_{\min}\) — минимальная температура включения модели из каталога
секционирования (сейчас −40 °C у всех моделей). Отдельного паспортного
ограничения по температуре окружающей среды в power-каталоге нет.

Если пригодных марок нет — `ELECTRICAL_CABLE_TEMPERATURE_LIMIT_EXCEEDED` с
уточнением: `ambient_below_minimum`, `product_above_maximum` либо
`temperature_combination_unsupported` (каждое ограничение достижимо, но ни
одна марка не проходит оба сразу).

Температура включения \(T_{\text{вкл}}\) в проверке пригодности **не
участвует** — она используется только при выборе строки секционирования
(раздел 10).

---

## 8. Подбор марки и числа ниток

### 8.1. Автоматический режим

Кандидаты — все температурно-пригодные полные марки BOM; для каждой
проверяются нитки \(N = 1, 2, 3\):

\[
P_{\text{каб}} \cdot K_{\text{нав}} \cdot N \ge P_{\text{треб}}
\]

Из прошедших выбирается минимальный по лексикографическому ключу §6.13;
с 2026-08-16 ничьи разрешаются явными рангами (DEC-19):

1. \(N\) по возрастанию;
2. \(P_{\text{каб}}\) по возрастанию;
3. \(P_{\text{каб}} \cdot N\) по возрастанию;
4. температурный класс серии: ТТН → ТТВ → ТТХ;
5. исполнение: «-СТ» раньше «-СР»;
6. полная марка по алфавиту (стабилизатор).

Если ни один кандидат не прошёл — `ELECTRICAL_CABLE_POWER_INSUFFICIENT`.
Порядок ошибок соответствует §6.13: сначала температуры, затем мощность.

Следствия сортировки в текущих каталогах:

- при равной паспортной мощности серию решает температурный класс
  (30 Вт/м есть у ТТВ и ТТХ — выбирается «30ТТВ2-СР» как более низкий класс);
- у базовых моделей с двумя исполнениями («-СТ» и «-СР», серия ТТН)
  автоподбор берёт стандартное «-СТ»; «-СР» достижимо только ручной маркой.
  Выбор исполнения по умолчанию помечается в результате
  (`cable.execution_source = "default"`) и предупреждением
  `ELECTRICAL_EXECUTION_DEFAULTED`.

### 8.2. Ручной режим

Заданная марка ищется по точному `full_mark`; легаси-марка «ТЛТ-…» —
отказ `ELECTRICAL_LEGACY_CABLE_MARK_UNSUPPORTED`; не найдена —
`ELECTRICAL_CABLE_NOT_FOUND`. Марка не подменяется: она проходит те же
температурные и мощностные проверки, и при провале сохраняется ошибка.
Если нитки не заданы, для ручной марки принимается \(N = 1\) (§6.13).
Заданное пользователем число ниток (1–3) фиксируется без перебора.

Источник выбора ниток сохраняется: `manual` (задано), `manual_default`
(ручная марка без ниток), `auto` (перебор).

### 8.3. Установленная мощность

\[
P_{\text{уст}} = P_{\text{каб}} \cdot K_{\text{нав}} \cdot N
\]

Навив и нитки увеличивают установленную мощность на метр объекта — это
правило §6.13 (`P_installed_per_meter = P_cable * K_nav * N`).

---

## 9. Требуемая длина кабеля

\[
L_{\text{треб}} = L_{\text{базы}} \cdot K_{\text{нав}} \cdot N
\]

Для трубы \(L_{\text{базы}}\) — эффективная длина теплорасчёта (раздел 4.1),
для резервуара — длина укладки по поверхности (раздел 4.2).

---

## 10. Секционирование (§6.14)

### 10.1. Токовое ограничение

Если в настройках проекта задан ручной \(I_{\text{доп}}\) — используется он
(источник `project_setting`). Иначе предел выводится из выбранной строки
каталога:

\[
I_{\text{доп}} = L_{\text{макс}} \cdot I_{\text{ст.уд}}
\]

с источником `section_catalog_derived`. В производном режиме токовое
ограничение по построению совпадает с паспортным ограничением длины:
\(L_{\text{ток}} = L_{\text{макс}}\), то есть фактически не сужает секцию.

### 10.2. Длины секций

\[
L_{\text{ток}} = \frac{I_{\text{доп}}}{I_{\text{ст.уд}}}
\qquad
L_{\text{огр}} = \operatorname{округл.вниз}_{0{,}001}
\bigl(\min(L_{\text{макс}}, L_{\text{ток}})\bigr)
\]

Округление вверх запрещено (§6.14): оно могло бы превысить максимальную
длину или допустимый стартовый ток.

\[
n = \left\lceil \frac{L_{\text{треб}}}{L_{\text{огр}}} \right\rceil
\qquad
L_{\text{сек}} = L_{\text{огр}}
\qquad
L_{\text{факт}} = L_{\text{сек}} \cdot n
\qquad
L_{\text{доп}} = L_{\text{факт}} - L_{\text{треб}}
\]

Все секции одинаковы; последняя не уменьшается на величину остатка
(§6.14: «67/67/66 — некорректно, 67×3 — корректно»). Длина каждой секции
всегда равна \(L_{\text{огр}}\) — в том числе когда объекту хватает одной
короткой секции: объект с \(L_{\text{треб}} = 10\) м при
\(L_{\text{огр}} = 67\) м получает одну секцию 67 м и \(L_{\text{доп}} = 57\) м
(поведение зафиксировано тестом
`test_short_object_still_gets_one_full_equal_section`).

### 10.3. Заказная длина

\[
L_{\text{заказ}} = \operatorname{округл.вверх}_{0{,}001}
\bigl(L_{\text{факт}} \cdot 1{,}10\bigr)
\]

Монтажный запас 10 % применяется к фактической длине секций (включая
\(L_{\text{доп}}\)), а не к требуемой.

---

## 11. Электрические параметры

Все мощности и токи считаются от фактической длины секций:

\[
P_{\text{сек}} = P_{\text{каб}} \cdot L_{\text{сек}}
\qquad
P_{\text{общ}} = P_{\text{сек}} \cdot n = P_{\text{каб}} \cdot L_{\text{факт}}
\]

\[
I_{\text{ст.сек}} = I_{\text{ст.уд}} \cdot L_{\text{сек}}
\qquad
I_{\text{ст.общ}} = I_{\text{ст.сек}} \cdot n
\]

\[
I_{\text{раб.сек}} = \frac{P_{\text{сек}}}{U}
\qquad
I_{\text{раб.общ}} = I_{\text{раб.сек}} \cdot n
\]

\(U = 230\) В (раздел 3.5). Проверка \(I_{\text{ст.сек}} \le I_{\text{доп}}\)
выполняется дважды: при построении плана и в финальном гейте.

В сохранённом результате поля с префиксом `section_*`
(`section_start_current_a`, `section_working_current_a`, `section_power_w`)
содержат **суммарные** значения по всем секциям; значения одной секции лежат
в `section_plan.*_per_section_*` и в списке `sections`.

---

## 12. Финальный гейт (§6.14)

Результат получает `status=ready` только после полного набора проверок
(`tt_final_gate.py`); любой провал — `ELECTRICAL_FINAL_GATE_FAILED`:

- марка и серия определены;
- напряжение положительно и совпадает с напряжением плана секций;
- нитки — целое 1–3;
- \(n > 0\), \(L_{\text{сек}} > 0\), \(L_{\text{сек}} \le L_{\text{макс}}\);
- \(I_{\text{ст.сек}} \le I_{\text{доп}}\);
- \(L_{\text{факт}} \ge L_{\text{треб}}\);
- число секций в списке равно \(n\); каждая секция несёт одинаковые
  длину, напряжение, мощность, рабочий и стартовый токи;
- \(P_{\text{треб}} > 0\) и \(P_{\text{уст}} \ge P_{\text{треб}}\);
- у каждого из трёх каталогов есть version либо checksum.

Допуски сравнений: длины 0,0005 м; мощности 0,01 Вт; токи 0,001 А.

---

## 13. Состав и жизненный цикл результата

### 13.1. Ключевые поля результата

| Поле | Содержание |
|---|---|
| `cable.mark` / `cable_mark` | Полная марка (например, `30ТТВ2-СР`) |
| `cable.base_model`, `cable.series`, `cable.suffix` | Базовая модель, серия, исполнение |
| `cable.nomenclature_code` | Номенклатурный код BOM |
| `cable.passport_power_w_per_m` / `power_per_meter` | \(P_{\text{каб}}\) |
| `cable.selection_source` | `auto` / `manual` |
| `cable.execution_source` | Источник исполнения «-СТ»/«-СР»: `manual` / `default` / `single` (DEC-19) |
| `warnings` | Предупреждения результата (`ELECTRICAL_POWER_CATALOG_PROVISIONAL`, `ELECTRICAL_EXECUTION_DEFAULTED`) |
| `layout.winding_factor` / `winding_coefficient` | \(K_{\text{нав}}\) |
| `layout.required_installed_length_m` | \(L_{\text{треб}}\) |
| `layout.actual_installed_length_m` / `installed_cable_length` | \(L_{\text{факт}}\) |
| `layout.excess_installed_length_m` / `section_l_excess_m` | \(L_{\text{доп}}\) |
| `layout.required_order_length_m` / `order_cable_length` | \(L_{\text{заказ}}\) |
| `num_circuits`, `threads`, `thread_selection_source` | Нитки и источник выбора |
| `section_plan.count`, `.length_m`, `.l_max_m`, `.l_tok_m`, `.l_ogr_m` | План секций |
| `section_plan.max_start_current_a`, `.max_start_current_source` | Применённый \(I_{\text{доп}}\) и его источник |
| `sections[]` | Одинаковые секции: длина, мощность, токи, напряжение |
| `electrical.required_power_per_meter_w` | \(P_{\text{треб}}\) |
| `electrical.installed_power_per_meter_w` / `installed_power_per_meter` | \(P_{\text{уст}}\) |
| `electrical.total_power_w` / `total_power` | \(P_{\text{общ}}\) |
| `electrical.working_current_a` / `current` | \(I_{\text{раб.общ}}\) |
| `electrical.start_current_a` / `start_current` | \(I_{\text{ст.общ}}\) |
| `catalogs.power/section/bom` | Снимки каталогов с версиями и checksum |
| `provenance` | Снимки объекта/тепла, источники входов, версии, фингерпринты |
| `resolved_inputs`, `input_sources` | Канонические входы и источник каждого |

Фингерпринт формулы (`electrical-tt-v3-case1-r6` + sha256 контракта) и
фингерпринт расчёта (hash входов, источников и каталогов) сохраняются в
`provenance`.

### 13.2. Устаревание сохранённого результата

Сохранённый результат помечается stale-оверлеем
(`ELECTRICAL_RECALCULATION_REQUIRED`) без перезаписи снимка, если: марка
легаси «ТЛТ-…», отсутствует/некорректно напряжение, не совпадает версия или
фингерпринт формулы, у какого-либо каталога нет checksum, отсутствует
применённый \(I_{\text{доп}}\). Изменение тепловых результатов объекта также
помечает электрорасчёты объекта устаревшими. На проект допускается до
4 электрорасчётов (ЭР); переопределения и результаты изолированы по ЭР.

### 13.3. Ошибка как результат

Ошибка расчёта сохраняется как результат объекта (код, сообщение,
категория, provenance доступных каталогов) и переживает перезагрузку
страницы — по §6.13 «ошибка должна сохраняться как результат
электрорасчёта». К деталям ошибок подбора добавляется контекст резолвнутых
входов селектора (температура среды, температура включения, наружный
диаметр, климатические `climate_city` / `climate_temperature_basis` /
`climate_policy_rule`) — инженер видит, с какими именно значениями расчёт
отказал.

Основные коды: `ELECTRICAL_HEAT_LOSS_REQUIRED`,
`ELECTRICAL_REQUIRED_POWER_INVALID`, `ELECTRICAL_INPUT_REQUIRED`,
`ELECTRICAL_INPUT_INVALID`, `ELECTRICAL_INPUT_RETIRED`,
`ELECTRICAL_THREAD_COUNT_INVALID`, `ELECTRICAL_SELECTION_POLICY_UNSUPPORTED`,
`ELECTRICAL_WINDING_PITCH_INVALID`,
`ELECTRICAL_WINDING_FACTOR_LIMIT_EXCEEDED`,
`ELECTRICAL_CABLE_NOT_FOUND`, `ELECTRICAL_LEGACY_CABLE_MARK_UNSUPPORTED`,
`ELECTRICAL_CABLE_TEMPERATURE_LIMIT_EXCEEDED`,
`ELECTRICAL_CABLE_POWER_INSUFFICIENT`,
`ELECTRICAL_SECTION_CATALOG_ROW_NOT_FOUND`,
`ELECTRICAL_SECTION_PLAN_INVALID`, `SECTION_CURRENT_LIMIT_REQUIRED`,
`ELECTRICAL_NOMINAL_VOLTAGE_INVALID`, `ELECTRICAL_TANK_SHAPE_UNSUPPORTED`,
`ELECTRICAL_TANK_LAYOUT_INPUT_UNSUPPORTED`, `ELECTRICAL_FINAL_GATE_FAILED`,
`ELECTRICAL_CATALOG_ROW_INVALID`, `ELECTRICAL_CATALOG_SOURCE_UNREGISTERED`,
`SPEC_CABLE_NOMENCLATURE_MISSING`, `ELECTRICAL_MOCK_INPUTS_NOT_ALLOWED`.

---

## 14. Отличия от легаси-алгоритма («Алгоритм Самрег. трубы»)

Блок-схема легаси-алгоритма (температурно-зависимая мощность
\(P_i(T_3) = Q_1 T_3 + Q_2\), выбор серии каскадом Т1/Т2, суффикс «СР» по
признаку агрессивного продукта, нитки \(N=\lceil P_{\text{об}}/P_i\rceil\)
без ограничения) в текущем коде **не реализована**. Действующая методика —
Кейс ред. 4 §6.13–6.14: паспортная константная мощность, температурные
ограничения из каталогов, нитки 1–3, суффикс — часть полной марки BOM
(при ничьей исполнение по умолчанию «-СТ», DEC-19 — совпадает с ветвью
легаси-схемы для неагрессивного продукта).
Поля легаси-входов (пропарка, агрессивность, температура поддержания)
из электрического контракта исключены (раздел 3.6).

---

## 15. Сквозной пример

Труба: базовые теплопотери \(q = 25\) Вт/м, \(K = 1{,}1\), эффективная длина
50 м, прямая укладка, \(T_{\text{прод}} = 40\) °C, \(T_{\text{среды}} = -30\) °C,
\(T_{\text{вкл}} = -20\) °C, \(U = 230\) В, автоподбор, проектный
\(I_{\text{доп}}\) не задан. Числа получены прогоном фактических формул на
текущих каталогах.

1. Требуемая мощность: \(P_{\text{треб}} = 25 \cdot 1{,}1 = 27{,}5\) Вт/м;
   \(K_{\text{нав}} = 1\).
2. Температурная пригодность: все 14 моделей проходят
   (\(-30 \ge -40\)); по \(T_{\text{прод}} = 40 \le 65/120/150\) — все.
3. Кандидаты при \(N=1\): \(P_{\text{каб}} \ge 27{,}5\) → 31ТТН2 (31),
   30ТТВ2 (30), 45ТТВ2, 60ТТВ2, 30ТТХ2 (30), 45ТТХ2, 60ТТХ2, 75ТТХ2, 90ТТХ2.
4. Сортировка: минимальная мощность 30 у 30ТТВ2 и 30ТТХ2; более низкий
   температурный класс серии → **`30ТТВ2-СР`**, \(N = 1\), серия ТТВ
   (у ТТВ единственное исполнение, `execution_source = "single"`).
5. Требуемая длина: \(L_{\text{треб}} = 50 \cdot 1 \cdot 1 = 50\) м.
6. Строка секционирования 30ТТВ2 при −20 °C:
   \(L_{\text{макс}} = 95\) м, \(I_{\text{ст.уд}} = 0{,}195\) А/м.
7. Производный предел: \(I_{\text{доп}} = 95 \cdot 0{,}195 = 18{,}525\) А;
   \(L_{\text{ток}} = 95\) м; \(L_{\text{огр}} = 95\) м.
8. Секции: \(n = \lceil 50/95 \rceil = 1\); \(L_{\text{сек}} = 95\) м;
   \(L_{\text{факт}} = 95\) м; \(L_{\text{доп}} = 45\) м.
9. Заказ: \(L_{\text{заказ}} = 95 \cdot 1{,}1 = 104{,}5\) м.
10. Мощности: \(P_{\text{уст}} = 30\) Вт/м;
    \(P_{\text{общ}} = 30 \cdot 95 = 2850\) Вт.
11. Токи: \(I_{\text{ст}} = 0{,}195 \cdot 95 = 18{,}525\) А (= \(I_{\text{доп}}\));
    \(I_{\text{раб}} = 2850 / 230 = 12{,}391\) А.
12. Финальный гейт: все проверки проходят; результат `ready`.

Вариант с навивом: та же труба Ø108 мм с шагом навива 500 мм даёт
\(K_{\text{нав}} = \sqrt{1+(\pi\cdot108/500)^2} = 1{,}208502\)
(максимум для Ø108 — 1,4, проходит); установленная мощность на метр
\(25 \cdot 1{,}2085 = 30{,}2\) Вт/м — теперь достаточно 25ТТН2, и автоподбор
выбирает **`25ТТН2-СТ`** с длиной \(50 \cdot 1{,}2085 = 60{,}425\) м
(у 25ТТН2 два исполнения; взято стандартное «-СТ» по DEC-19, в результате
`cable.execution_source = "default"` и предупреждение
`ELECTRICAL_EXECUTION_DEFAULTED`).

---

## 16. Справочно: зафиксированные особенности текущего поведения

Подраздел описывает наблюдения «как есть», выявленные при сверке кода с
§6.13–6.14 и каталогами. Оценок и рекомендаций не содержит.

### 16.1. Длина секции всегда равна Lогр

\(L_{\text{сек}} = L_{\text{огр}}\) независимо от \(L_{\text{треб}}\).
Для короткого объекта единственная секция имеет полную длину
\(L_{\text{огр}}\): в примере раздела 15 объекту на 50 м кабеля назначается
секция 95 м, и все итоговые величины — мощность (2850 Вт вместо
50 м × 30 Вт/м = 1500 Вт), рабочий и стартовый токи, заказная длина
(104,5 м) — считаются от 95 м. Текст §6.14 задаёт равенство секций и запрет
уменьшать последнюю секцию; его числовой пример (200 м / 67 м → 3×67)
совпадает с кодом, случай \(L_{\text{треб}} < L_{\text{огр}}\) в тексте кейса
отдельно не описан. Поведение зафиксировано тестом как характеристика.

### 16.2. Tмин марки — производная от каталога секционирования

Паспортного ограничения по температуре окружающей среды у марок нет;
\(T_{\min}\) выводится как минимальная температура включения модели в
каталоге секционирования (−40 °C у всех моделей). Следствие: при расчётной
температуре среды ниже −40 °C (например, климатическая политика труб
D < 100 мм подставляет абсолютный минимум — для строки «Москва» −43 °C)
все марки отклоняются с `ambient_below_minimum`, при том что температура
включения объекта ограничена формой диапазоном −40…10 °C и в проверке
пригодности не участвует.

### 16.3. Температура среды и температура включения не связаны

\(T_{\text{среды}}\) (проверка пригодности) и \(T_{\text{вкл}}\) (выбор
строки секционирования) — независимые входы; соотношение между ними не
проверяется. Форма допускает \(T_{\text{вкл}}\) теплее расчётной температуры
среды; строка секционирования при этом соответствует более тёплому пуску
(меньший \(I_{\text{ст.уд}}\), длиннее \(L_{\text{огр}}\)).

### 16.4. Исполнение по умолчанию — «-СТ» (DEC-19, 2026-08-16)

Признак агрессивной среды из контракта исключён; правило выбора исполнения
закреплено DEC-19: при автоподборе для базовых моделей ТТН, имеющих оба
исполнения, берётся стандартное «-СТ», исполнение «-СР» достижимо только
ручным выбором полной марки. Выбор по умолчанию помечается
`cable.execution_source = "default"` и предупреждением
`ELECTRICAL_EXECUTION_DEFAULTED`; при единственном исполнении —
`"single"`, при ручной марке — `"manual"`. Поля формы «Среда» и
«Температурная группа» на выбор по-прежнему не влияют.
До 2026-08-16 ничья решалась алфавитом полной марки и всегда давала «-СР»
(«Р» < «Т») — сохранённые ранее результаты могли зафиксировать «-СР».

### 16.5. Производный Iдоп не ограничивает секцию

Без проектного \(I_{\text{доп}}\) предел выводится как
\(L_{\text{макс}} \cdot I_{\text{ст.уд}}\), из чего
\(L_{\text{ток}} = L_{\text{макс}}\): токовое ограничение активно только при
ручном проектном значении.

### 16.6. Напряжение — константа трёх уровней

Фронтенд жёстко отправляет 230 В в пакетных запросах (значение из настроек
проекта интерфейсом не потребляется), настройки проекта не принимают иное
напряжение в PATCH, БД ограничена CHECK-ом 230. Итоговое `input_sources`
для напряжения при пакетном пересчёте показывает `explicit_request`.

### 16.7. Резервуар: два коэффициента запаса

Снятие теплового запаса делит `total_heat_loss_design` на
\(K_{\text{тепл}}\) из теплового результата, а электрорасчёт умножает на
разрешённый резолвером \(K\) (который может быть переопределён запросом или
assignment). При \(K \ne K_{\text{тепл}}\) слагаемое \(Q_{\text{доп}}\)
масштабируется отношением \(K/K_{\text{тепл}}\).

### 16.8. Длина трубы включает локальные элементы

База длины кабеля трубы — `effective_length` теплорасчёта
(\(L + n_{\text{лок}} L_{\text{экв}}\)): эквивалентная длина локальных
элементов увеличивает требуемую и заказную длину кабеля.

### 16.9. Смысл полей `section_*` в плоском результате

Плоские поля `section_start_current_a`, `section_working_current_a`,
`section_power_w` содержат суммарные значения по объекту, а не значения
одной секции; значения одной секции — в `section_plan` и `sections[]`.

### 16.10. Поля формы, не участвующие в TT-расчёте

«Температура поддержания», «Пропарка», «Температура пропарки», «Среда»,
«Температурная группа» собираются и сохраняются в параметрах объекта, но
TT-расчёт их не читает (контракт отклоняет их только при явной передаче в
электрический запрос). Температурные коэффициенты `q1`/`q2` и поля
`max_vapor_temp`, `voltage` power-каталога расчётом также не используются.

### 16.11. Навив увеличивает установленную мощность

\(P_{\text{уст}} = P_{\text{каб}} K_{\text{нав}} N\) — по §6.13 навив
одновременно увеличивает длину кабеля на метр трубы и засчитывается в
покрытие требуемой мощности, поэтому при навиве может пройти более слабая
марка (пример в разделе 15).

### 16.12. Монтажный запас применяется к Lфакт

10 % заказного запаса считается от фактической длины секций (уже включающей
\(L_{\text{доп}}\)), а не от требуемой длины \(L_{\text{треб}}\)
(§6.13 определяет запас от установленной длины; установленной в результате
считается \(L_{\text{факт}}\)).
