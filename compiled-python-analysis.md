# TLT HeatCalc — Стоит ли переходить на компилируемый Python?

**Дата:** 2026-05-10  
**Вопрос:** Есть ли смысл в Cython / PyPy / Nuitka / mypyc?

---

## Короткий ответ: нет.

Текущие расчёты — 2–8 мс на объект. Batch 400 объектов — 94 мс (ваши замеры).
Ускорение расчётов в 4 раза не изменит пользовательский опыт ни на миллисекунду,
потому что bottleneck'ом является не CPU, а ожидание ответа от БД и сети.

---

## Что даст каждый вариант для HeatCalc

### Cython

Компиляция `.py` → `.c` → нативный модуль. Требует явных type annotations
(`cdef double x`), отдельной сборки, усложняет CI/CD.

**Для текущих формул:** `math.log` и `math.sqrt` — это и так C (libm).
Cython ускорит только Python-обвязку (вызов функции, распаковка словарей) —
с 2–8 мс до ~1–3 мс. Разница в 3–5 мс на объект. Пользователь не заметит.

**Для F-02 (Crank-Nicolson):** ускорение петли на 2–4×. Было 2–8 сек → стало
0.5–4 сек. Лучше, но всё ещё не интерактивно. На этом этапе Go даст 80–300 мс
без усложнения Python-сборки.

**Вердикт:** ❌ Не оправдано.

### PyPy

JIT-компиляция во время выполнения. Совместим с чистым Python, проблемы
с C-расширениями.

**Проблемы совместимости со стеком HeatCalc:**

| Компонент | Совместимость с PyPy | Риск |
|---|---|---|
| `asyncpg` | Чистый Python? Нет — бинарный протокол через C. | 🔴 Сломается |
| `bcrypt` | C-расширение. Есть pure-Python fallback, но медленный. | 🟡 Будет работать, но медленнее |
| `weasyprint` | Зависит от Pango/Cairo (C). | 🔴 Сломается |
| `orjson` / `ujson` | C-расширения. | 🟡 Нужен fallback на stdlib json |
| FastAPI + uvicorn | Работает. | 🟢 |

PyPy ускорит чистый Python в 3–8× ценой несовместимости с половиной стека.
Отладка проблем займёт недели. Не стоит.

**Вердикт:** ❌ Слишком высокий риск несовместимости.

### Nuitka

Компиляция всего приложения в один бинарник. Основное преимущество —
упрощение деплоя (нет необходимости в Python-рантайме на целевой машине).
Но HeatCalc уже упакован в Docker — проблемы деплоя нет.

Скорость: +10–30% на чистом Python (Nuitka не JIT-компилирует, а транслирует
в C++, который компилируется в нативный код — прирост скромный).
Время сборки: 5–15 минут на каждое изменение.

**Вердикт:** ❌ Не даёт значимого ускорения, усложняет сборку.

### mypyc

Компиляция type-annotated Python-модулей в C-расширения. Используется
самим mypy. Требует полного покрытия type hints на всех функциях.

**Для текущих формул:** ускорение 2–4× на чистом Python-коде.
2–8 мс → 0.5–4 мс. Разница неощутима.

Плюс: остаётся совместимость с обычным Python (есть fallback на
интерпретируемый режим). Не ломает зависимости.

Минус: требует аннотирования **всех** функций и всех внутренних переменных
в расчётных модулях. Для `pipe.py` это ~200 строк аннотаций. Поддержка
типов должна быть идеальной — mypyc не прощает `Any`.

**Вердикт:** 🟡 Самый реалистичный вариант, но не оправдан при текущей
скорости. Имеет смысл только для F-02 (Crank-Nicolson).

---

## Сравнительная таблица

| Критерий | Сейчас (CPython) | Cython | PyPy | Nuitka | mypyc |
|---|---|---|---|---|---|
| Скорость (текущие формулы) | 2–8 мс | 1–3 мс | 0.5–2 мс | 1.5–6 мс | 0.5–4 мс |
| Скорость (F-02 transient) | 2–8 сек | 0.5–2 сек | 0.3–1 сек | 1.5–6 сек | 0.5–4 сек |
| Совместимость с asyncpg | ✅ | ✅ | 🔴 Нет | ✅ | ✅ |
| Совместимость с weasyprint | ✅ | ✅ | 🔴 Нет | ✅ | ✅ |
| Сложность внедрения | — | Средняя | Высокая | Средняя | Низкая |
| Затраты на поддержку | — | +1 инженер | +1 инженер | +0.5 инженера | +0.2 инженера |
| Эффект для пользователя | — | Незаметен | Незаметен | Незаметен | Незаметен |

---

## Когда компиляция станет нужна

**Порог:** когда время расчёта на один объект превышает 100 мс **и**
пользователь ждёт результат интерактивно (не в фоне).

По текущим формулам это маловероятно. Порог будет пересечён только при
реализации:

| Фича | Время (Python) | Порог? | Что делать |
|---|---|---|---|
| F-01 Теплогидравлика | 50–150 мс | 🟡 Погранично | Оставить Python |
| F-02 Crank-Nicolson | 2–8 сек | 🔴 Да | **Go**, не компилируемый Python |
| F-03 NSGA-II | 30–90 сек | 🔴 Да | **Go**, не компилируемый Python |

Для F-02 и F-03 я уже рекомендовал Go (см. `architecture-decisions.md`).
Причина: Go даёт ускорение в 20–50× против 2–4× от mypyc/Cython **и**
не создаёт проблем совместимости с Python-стеком (Go-сервис — отдельный
процесс, вызывается по gRPC/REST).

---

## Итог

| Вопрос | Ответ |
|---|---|
| Есть ли смысл в компилируемом Python **сейчас**? | **Нет.** Расчёты быстрые, bottleneck не в CPU. |
| Есть ли смысл для **F-02/F-03**? | **Нет.** Go даст в 5–10× больше ускорения без проблем совместимости. |
| Самый реалистичный вариант? | mypyc. Но всё равно не оправдан при текущих скоростях. |
| Когда пересмотреть решение? | Когда появится F-02 и бенчмарк покажет > 1 сек на типичном проекте. |

---

*Документ подготовлен: 2026-05-10  
Основан на: ваших замерах (400 объектов / 94 мс), анализе стека зависимостей, comparison матрице.*
