# TC-SPEC: Спецификация

## TC-SPEC-01: Генерация пустой спецификации (нет объектов)

**Автоматизировано:** ✅ `test_specifications.py::TestSpecification::test_generate_empty_specification`

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | Создать пустой проект | `project_id`, 0 объектов |
| 2 | `POST /api/v1/specifications/{project_id}/generate` | HTTP 201 |
| 3 | Проверить `items` | `[]` (пустой массив) |
| 4 | Повторный вызов | HTTP 201, возвращается обновлённая спецификация |

---

## TC-SPEC-02: Получение спецификации после генерации

**Автоматизировано:** ✅ `test_specifications.py::TestSpecification::test_get_specification_after_generate`

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | `POST /api/v1/specifications/{id}/generate` | HTTP 201 |
| 2 | `GET /api/v1/specifications/{project_id}` | HTTP 200 |
| 3 | Проверить поля ответа | `id`, `project_id`, `variant_number`, `items` |

---

## TC-SPEC-03: Генерация спецификации с электрическими расчётами

**Автоматизировано:** ✅ (unit) `test_spec_builder.py::TestSpecBuilder::test_groups_cable_by_mark`

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | Создать 2 объекта типа `pipe` | — |
| 2 | Выполнить электрорасчёт для обоих с `cable_mark=ТЛТ-25` (длина 30 и 20 м) | — |
| 3 | `POST /api/v1/specifications/{project_id}/generate` | HTTP 201 |
| 4 | В `items` найти строку с категорией «Кабель» и артикулом «ТЛТ-25» | `quantity = 50.0` |

---

## TC-SPEC-04: Структура позиций спецификации

**Автоматизировано:** ✅ (unit) `test_spec_builder.py::TestSpecBuilder::test_adds_accessories`

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | Сгенерировать спецификацию с объектами | HTTP 201 |
| 2 | Проверить категории в `items` | Есть «Управление», «Защита» |
| 3 | Каждая позиция | `category`, `name`, `article`, `unit`, `quantity` |

---

## TC-SPEC-05: Сортировка позиций

**Автоматизировано:** ✅ (unit) `test_spec_builder.py::TestSpecBuilder::test_items_sorted`

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | Сгенерировать спецификацию | — |
| 2 | Проверить порядок позиций | Отсортированы по `(category, name)` |

---

## TC-SPEC-06: Спецификация для несуществующего проекта

**Автоматизировано:** ❌ (мануальный)

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | `POST /api/v1/specifications/00000000-0000-0000-0000-000000000000/generate` | HTTP 404 |
| 2 | `GET /api/v1/specifications/00000000-0000-0000-0000-000000000000` | HTTP 404 |

---

## TC-SPEC-07: Доступ к спецификации чужого проекта

**Автоматизировано:** ❌ (мануальный)

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | Создать проект от session_A и сгенерировать спецификацию | `spec_id` |
| 2 | `GET /api/v1/specifications/{project_id}` с session_B | HTTP 403 |
