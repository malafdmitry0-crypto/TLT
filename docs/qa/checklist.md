# Чек-лист перед релизом

## Автоматические проверки

- [ ] `make test-backend` — **113/113 passed**
- [ ] `make test-frontend` — все Vitest-тесты зелёные
- [ ] `make lint-backend` — ruff: 0 ошибок
- [ ] `make lint-frontend` — eslint: 0 ошибок

## Smoke (базовая работоспособность)

- [ ] `make dev` стартует без ошибок
- [ ] `GET /health` → `{"status": "ok"}`
- [ ] Swagger UI открывается: http://localhost:8000/docs
- [ ] Фронтенд открывается: http://localhost:3003
- [ ] Первый admin-пользователь создаётся автоматически

## Авторизация

- [ ] Гостевой вход работает (кнопка «Пользователь»)
- [ ] Логин сотрудника работает
- [ ] Логин с неверным паролем → 401
- [ ] Неавторизованный доступ к `/api/v1/projects` → 401
- [ ] Сотрудник не может войти в /admin → 403
- [ ] Refresh token обновляет access token
- [ ] GET /auth/me возвращает корректные данные

## Проекты

- [ ] Гость создаёт проект → `session_id` заполнен
- [ ] Гость видит только свои проекты
- [ ] Сотрудник видит все проекты
- [ ] Создание / редактирование / удаление работает
- [ ] Переход в статус `completed` работает

## Объекты и расчёты

- [ ] Добавление объекта типа `pipe` с валидными параметрами → `is_valid: true`, `results` заполнены
- [ ] Добавление объекта с невалидным материалом изоляции → `is_valid: false`
- [ ] Обновление параметров → автопересчёт (значение меняется)
- [ ] Объект типа `tank` рассчитывается корректно
- [ ] Batch-пересчёт всех объектов проекта работает

## Электротехнический расчёт

- [ ] Расчёт саморегулирующегося кабеля ТЛТ-25 → подбирается кабель
- [ ] Неизвестная марка → автоматический подбор
- [ ] Требуемая мощность выше максимума → ошибка 400/422

## Спецификация

- [ ] Генерация спецификации для проекта без объектов → `items: []`
- [ ] После генерации `GET /specifications/{id}` возвращает данные

## Отчёты

- [ ] `GET /reports/{id}/preview` → HTML с тегом `<html`
- [ ] Гость не может экспортировать → 403
- [ ] Сотрудник: экспорт XLSX → файл скачивается, Content-Type корректный
- [ ] Экспорт PDF → файл скачивается
- [ ] Экспорт DOCX → файл скачивается

## Администрирование

- [ ] Список пользователей доступен только admin
- [ ] Создание нового сотрудника
- [ ] Дублирующийся email → 400
- [ ] Деактивация пользователя (is_active → false)
- [ ] Обновление коэффициента (например, `wind_factor → 1.3`)
- [ ] Сотрудник не может обновить коэффициент → 403
- [ ] Создание / редактирование / удаление кабеля
- [ ] Создание / редактирование / удаление аксессуара

## Справочники

- [ ] `GET /references/climate` — доступен гостю, содержит «Москва»
- [ ] `GET /references/insulation` — содержит `mineral_wool`
- [ ] `GET /references/cables` — содержит «ТЛТ-25»
- [ ] `GET /references/cables/extended` — гость получает 403, сотрудник 200
- [ ] `GET /references/accessories/extended` — аналогично

## Граничные значения (регрессия)

- [ ] Диаметр трубы < 10,8 мм (0.0108 м) → 422
- [ ] Температура окружающей среды < −70°C → 422
- [ ] Температура продукта ≥ температуры среды → ошибка расчёта
- [ ] Глубина заложения < радиуса изоляции → ошибка расчёта
- [ ] Толщина стенки > радиуса трубы → ошибка расчёта
- [ ] Многослойная изоляция: 4 слоя → 422 (максимум 3)
