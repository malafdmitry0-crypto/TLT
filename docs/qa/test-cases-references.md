# TC-REF: Справочники

## TC-REF-01: Климатические регионы (публичный)

**Автоматизировано:** ✅ `test_references.py::TestReferences::test_climate_public`

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | `GET /api/v1/references/climate` с `X-Session-Id` гостя | HTTP 200 |
| 2 | Проверить структуру | Массив объектов с полем `region` |
| 3 | Проверить содержимое | Есть запись `{"region": "Москва", ...}` |
| 4 | Запрос без авторизации | HTTP 200 (публичный ресурс) |

---

## TC-REF-02: Материалы изоляции (публичный)

**Автоматизировано:** ✅ `test_references.py::TestReferences::test_insulation_public`

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | `GET /api/v1/references/insulation` | HTTP 200 |
| 2 | Проверить структуру | Массив с полями `material`, `conductivity` |
| 3 | Проверить содержимое | Есть `mineral_wool`, `foam_polyurethane` |

**Ожидаемые материалы:**

| Код | Наименование |
|-----|-------------|
| `mineral_wool` | Минеральная вата |
| `foam_polyurethane` | Пенополиуретан |
| `foam_glass` | Пеностекло |
| `polyurethane` | Пенополиуретан жёсткий |

---

## TC-REF-03: Встроенные кабели ТЛТ (публичный)

**Автоматизировано:** ✅ `test_references.py::TestReferences::test_cables_tlt_public`

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | `GET /api/v1/references/cables` | HTTP 200 |
| 2 | Проверить структуру | Массив с `model`, `power_per_meter` |
| 3 | Проверить содержимое | Есть `ТЛТ-25` |

---

## TC-REF-04: Расширенный каталог кабелей — требует сотрудника

**Автоматизировано:** ✅ `test_references.py::TestReferences::test_extended_cables_requires_employee`

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | `GET /api/v1/references/cables/extended` с `X-Session-Id` гостя | HTTP 403 |
| 2 | `GET /api/v1/references/cables/extended` с токеном сотрудника | HTTP 200 |
| 3 | Структура ответа | Массив с `cable_type`, `brand`, `model`, `power_per_meter`, commercial fields (`price_per_meter`, `stock_quantity_m`, `lead_time_days`, `supplier_priority`, `is_preferred`, `order_multiple_m`) |

---

## TC-REF-05: Встроенные аксессуары (публичный)

**Автоматизировано:** ❌ (мануальный)

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | `GET /api/v1/references/accessories` | HTTP 200 |
| 2 | Проверить структуру | Массив с `category`, `name` |

---

## TC-REF-06: Расширенные аксессуары — требует сотрудника

**Автоматизировано:** ❌ (мануальный)

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | `GET /api/v1/references/accessories/extended` с `X-Session-Id` гостя | HTTP 403 |
| 2 | С токеном сотрудника | HTTP 200 |
| 3 | Структура | `category`, `name`, `article`, `params`, `is_active` |

---

## TC-REF-07: Кэширование справочников (производительность)

**Автоматизировано:** ❌ (мануальный / нагрузочный)

| Шаг | Действие | Ожидаемый результат |
|-----|----------|---------------------|
| 1 | Первый `GET /api/v1/references/climate` после старта | Загружается из JSON-файла |
| 2 | Повторные запросы | Возвращаются из LRU-кэша, время ответа < 10 мс |
| 3 | Перезапуск сервиса | Кэш очищается, справочники перечитываются |
