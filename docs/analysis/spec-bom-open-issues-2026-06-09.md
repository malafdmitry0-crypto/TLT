# Полный BOM спецификации: открытые вопросы и техдолг

**Дата:** 2026-06-09
**Контекст:** аудит ветки `feat/full-specification-bom` (код vs бизнес-логика,
слабые места, документация). Часть находок исправлена кодом в тот же день —
см. раздел «Исправлено». Остальное требует бизнес-решения заказчика, сверки с
первоисточником или отдельной задачи.

Первоисточник: `ТНП/Расчет_спецификации_трубы_самрег29_05_26.xlsx`, лист
«Список материалов Самрег». Извлечённые формулы: `docs/audit/spec-bom-oracle.md`.

> **Resolution update 19.07.2026.** Этот файл сохраняется как историческое
> evidence аудита старого XLSX-builder. Для Phase 5 сильнее решения
> PDL-ER-29…41 из
> `docs/tnp/cases/guest-specification/product-decisions.md`: PDF всегда задаёт
> specification semantics/formulas, XLSX-only rule не переносится автоматически,
> а один canonical full mode заменяет пользовательский выбор basic/full.

| Старый вопрос | Статус после PDL | Каноническое решение / остаточный data gap |
|---|---|---|
| Б-1 | Data-blocked | PDL-ER-15/18/28: настоящее `Nсек` приходит только из доказанного section contract, не из `num_circuits`. |
| Б-2 | Contract resolved, data required | PDL-ER-33: temperature group — explicit catalog field, prefix inference запрещён. |
| Б-3 | Resolved | PDL-ER-08: ровно `57 мм` относится к `dтр ≥ 57`. |
| Б-4 | Resolved for Phase 5 semantics | PDL-ER-34: применяется row-driven алгоритм PDF; legacy priority `K2i > K1i` не является oracle. |
| Б-5 | Resolved as partial | PDL-ER-32: pipe accessories не применяются к tank; только доказанные позиции, остальное diagnostic partial. |
| Б-6 | Resolved | PDL-ER-02/29/34: один full mode, кабель BOM использует PDF order length; basic не product fallback. |
| Б-7 | Contract resolved, data required | PDL-ER-33: exact mark/code только из каталога, суффикс не синтезируется. |
| Б-8 | Resolved source priority | PDL-ER-34: PDF formula сильнее старого `L×Rгр`; XLSX-only поведение не наследуется. |
| Б-9 | Not approved for Phase 5 | Формула этикетки XLSX-only; по PDL-ER-34 она исключается до отдельного source mapping/PDL. |
| Б-10 | Resolved scope | PDL-ER-04/29: automatic BOM следует PDF; manual rows остаются employee/admin-only. |
| В-1 | Data-blocked | XLSX cable-entry baskets не становятся Phase 5 rule автоматически; нужен отдельный утверждённый mapping PDL-ER-34. |
| В-2/В-3 | Data-blocked | PDL-ER-33 требует explicit catalog identity/code; row order и одна только марка недостаточны. |
| В-4 | Resolved | PDL-ER-31: `Rгр` — отдельный project default `1.0`, применяется только explicit rules и не заменяет 10% order reserve. |

Отдельно PDF упоминает `Ex/Rгр` conditions коробок, но не даёт per-row values.
PDL-ER-35 запрещает считать пропуск как `не используется` или молча брать
старый XLSX: затронутые позиции остаются data-blocked до официальной матрицы.

---

## 1. Нужно бизнес-решение заказчика

| # | Вопрос | Текущее поведение кода | Риск |
|---|---|---|---|
| Б-1 | **`N,секц` vs нитки.** Лист «Список переменных» различает `N,секц` (кол-во нагревательных секций) и `Ns,секц` (число ниток алгоритма). Backend не секционирует кабель по строительной длине, правило секционирования самрега в контракте отсутствует. | `N,секц = num_circuits` (нитки) | На длинных трубах занижены КСН/КСВ, коробки, вводы |
| Б-2 | **Темп-класс ТЛТ.** Лист покрывает только ТТН/ТТВ/ТТХ; `T,секц` в источнике трёхзначный (низкий/средний/высокий), в коде — двухзначный. Высокотемпературные ТЛТ (до 190 °C) попадают в «низкотемпературные» КСН/КСР-1/ЛКС. | `_temp_class()`: всё, кроме ТТВ/ТТХ → `low` | Неверные комплекты для горячих ТЛТ |
| Б-3 | **Граница `dтр = 57 мм` ровно.** ТНП задаёт только `>57` и `<57`; случай равенства не определён (аналогично закрытому решению по границе темп-класса, `docs/audit/2026-06-07-temperature-class-boundary-decision.md`). | 57 относится к «малым» (СКВ 1202/1602) | Неоднозначность на границе |
| Б-4 | **Приоритет К2i над К1i при обоих «да».** Комбинированный случай в ТНП не описан; правило в коде изобретено. | `k2i_active` побеждает `k1i` в `_box_bucket` | Возможен неверный вариант СКВ |
| Б-5 | **BOM для резервуаров.** Источник — расчёт «трубы». Для танков diameter подставляется как dтр, height — как Lтр; формулы лент/этикеток/коробок по периметру бака никем не подтверждены. | Резервуары идут по pipe-алгоритму | Аксессуары бака приблизительные |
| Б-6 | **База длины кабеля basic vs full.** Basic берёт коммерческий `required_order_length` (строительные длины), full — `installed_cable_length × R,гр` по ТНП. Переключение режима меняет метры кабеля в одной записи. Какую длину заказывать в полном BOM? | Разные базы в двух режимах | Расхождение закупки кабеля |
| Б-7 | **Суффикс ТТВ/ТТХ в номенклатуре.** В номенклатурной базе ТНП (строки 17–26) ТТВ/ТТХ существуют **только** с `-СР`, а VSDX-алгоритм строит суффикс по агрессивности для любой серии. Код для неагрессивной среды выдаёт `30ТТВ2-СТ` — марки нет в номенклатуре (нет кода 001-002-xxx). Противоречие двух первоисточников. | Суффикс по агрессивности для всех серий | Марка вне номенклатурной базы |
| Б-8 | **Ленты и КСР от длины с резервом.** ЛКС/ЛКВ и ремкомплекты считаются от `L×R,гр`, хотя резервный кабель физически не монтируется. В источнике формула `Kr1` использует `L1` (длину с резервом) — видимо осознанно, но стоит подтвердить. | Лента/КСР включают резерв | Завышение ленты при R>1 |
| Б-9 | **`Lтр` для этикеток.** Сервис подставляет `results.effective_length` (с L,доп локальных элементов), тогда как `MrEl1` в ТНП считается от длины трубопровода. | effective_length приоритетнее pipe_length | Небольшое завышение этикеток |
| Б-10 | **КИП/шкаф/монтажные кабели/ЗИП** (строки 59+ источника) — проектные ручные позиции, в авто-BOM осознанно не входят. Подтвердить, что ручного добавления из расширенной БД достаточно. | Только ручные позиции | — |

## 2. Сверить с первоисточником / заказчиком (verify)

| # | Что | Где в коде |
|---|---|---|
| В-1 | Корзины Nk5/Nk6 (большие коробки с К2i) не получают кабельного ввода (`cable_entry_plastic/armored` = Nk1..Nk4+Nk7); Nk7 одновременно входит и в обычный ввод, и в УВИ. По коду логика не проверяема — нужна сверка с листом. | `full_builder.py`, `rule_values` |
| В-2 | Артикулы и коды СКВ-вариантов сопоставлены по порядку строк источника; связь с «Полный перечень продукции (для спецификаций).xlsx» (669 строк) не загружена. | `spec_accessories.json` |
| В-3 | Номенклатурные коды кабелей (001-001-001…008, различают -СТ/-СР) не назначаются кабельным позициям BOM — артикул = марка. | `full_builder.py`, кабельные позиции |
| В-4 | Значение `R,гр` по умолчанию и кто его задаёт (админ/проект) — открытый вопрос из `spec-bom-oracle.md`. | `SpecificationOptions.reserve_coefficient=1.0` |

## 3. Техдолг (отдельные задачи, бизнес-решение не нужно)

| # | Задача | Примечание |
|---|---|---|
| Т-1 | Перенести правила BOM в machine-readable registry QA-агента (секция `specification` в `qa-agent/examples/tlt-formulas.registry.yaml`) — пункт 4 `spec-bom-oracle.md`. | Сейчас в registry только basic-группировка |
| Т-2 | Golden-кейсы на суффикс -СТ/-СР в registry QA-агента. | Сейчас суффикс не встречается в registry |
| Т-3 | Регенерировать `backend/mutants/**` — там осталась старая (перепутанная) семантика суффикса; до этого mutation-прогон по `self_regulating.py` невалиден. | `scripts/formula-qa.sh mutation` |
| Т-4 | `formules.md` не содержит раздела ТТН/ТТВ/ТТХ и полного BOM, хотя объявлен «все формулы». Краткая версия есть в `docs/context/formulas-summary.md`. | Дописать или явно сослаться |
| Т-5 | Excel-экспорт не экранирует ведущий `=` в именах ручных позиций (formula injection). Вход только от сотрудника, риск низкий. | `reports/excel_generator.py` |
| Т-6 | `list_spec_accessory_rules()` возвращает shallow-copy с общими mutable-dict — мутация правила отравит `lru_cache` процесса. Сейчас никто не мутирует. | `reference_data/loader.py` |
| Т-7 | Тесты full BOM: добавить кейсы корзин с индикацией (Nk3/Nk4/Nk6/Nk9..Nk12), сервисного слоя `mode="full"` с резервуарами, `_temp_class` для нерусских строк. | Базовые ветки уже покрыты (см. «Исправлено») |
| Т-8 | `docs/api.md` / `docs/db_schema.md`: расширить описание спецификации (структура items, generation_mode/options). | Минимальное упоминание `mode` добавлено 2026-06-09 |

## 4. Исправлено кодом 2026-06-09 (для истории)

- **Упаковочный коэффициент (колонка I)** применяется: `package_factor` в
  `spec_accessories.json`, количество = `ceil(формула × коэф.)`. Ранее клей
  завышался ~в 7 раз, герметик — в 4, ХК30 — до 30, Z-профиль — в 2, а сам
  коэффициент был ошибочно записан как `mass_kg`.
- **Фильтр по типу кабеля**: BOM самрега больше не применяется к резистивным
  ТТ Р1/ТТ Р3 (и прочим типам); такие объекты учитываются в `skipped_objects`.
- **Квантование перед `ceil`**: 10 секций × R=1.3 больше не дают 14 вместо 13.
- **К2i по длине секции**: порог `L,К2i` сравнивается с `L,секц`
  (= installed/N), а не с суммарной длиной позиции; КСН-2/КСВ-2 считаются
  только по секциям, прошедшим порог (согласовано с корзинами коробок).
- **Валидация входов**: позиция с нулевой длиной не порождает аксессуары;
  `num_circuits` округляется и ограничивается снизу 1 (отрицательное значение
  одного объекта больше не уменьшает комплекты других).
- **Резервуары**: `diameter` подставляется как dтр (вместо молчаливого d=0 и
  «малого диаметра»); полноценный BOM бака — см. Б-5.
- **Режим генерации персистится** (`generation_mode`/`generation_options`,
  миграция 0026): «Пересчитать» и фоновая регенерация больше не подменяют
  полный BOM базовым; UI восстанавливает режим и опции после reload.
- **Гость + `mode=full` → 403** вместо тихого даунгрейда; ответ генерации
  содержит `mode` и `skipped_objects`, UI предупреждает о неполной спецификации.
- **UI**: добавлены опции К1i/К2i/Кiu/L,К2i на странице спецификации (ранее
  4 из 6 опций ТНП были недостижимы); `_is_aggressive` (мёртвый код) удалён;
  `readStorageJson` не падает на SecurityError; баннер ошибок электрорасчёта
  не показывает счётчик без текста.
- **Модалка выбора марки**: сохранённая марка с суффиксом, не совпадающим с
  текущим переключателем агрессивности (например, `-СР` при выключенной
  агрессивной среде), больше не теряется — добавляется fallback-опция
  «сохранён в расчёте», характеристики кабеля резолвятся по базовой модели.
