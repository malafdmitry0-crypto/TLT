# Реестр знания: что подтверждено, опровергнуто, исправлено, неизвестно

**Дата:** 2026-06-07 (сплошная перепроверка по коду/первоисточникам/справочникам)
**Назначение:** единый статус всех утверждений сессии. Уровни доверия:
**✅ проверено лично** (прочитан код/первоисточник в этой перепроверке) ·
**⚙️ агент** (только из отчёта субагента, лично не пересверено) ·
**❌ опровергнуто** · **🔧 исправлено** · **❓ неизвестно/нужны данные**.

---

## 1. ✅ Подтверждено лично — теплопотери (физика)

| Факт | Где в коде | Совпадение с ТНП |
|---|---|---|
| α_внеш = `11.6 + 7·√v`, clamp `[11.6, 52]`, помещение `9.0` | `pipe.py:55-58`, `tank.py:157-160` | ✅ DOCX |
| Ветер: `min(α·wind_k, 52)` | `pipe.py:315` | ✅ |
| R стенки = `ln(r_out/r_in)/(2π·λ)` | `pipe.py:149` | ✅ |
| R внеш надземно = `1/(2π·r_out·α)` | `pipe.py:196` | ✅ |
| R внеш подземно = `arccosh = ln(x+√(x²−1))/(2π·λ_гр)` | `pipe.py:211-212` | ✅ |
| K (safety_factor) применяется к Q, не к q; default `1.1` | `pipe.py:236-238,334`, `tank.py:263` | ✅ |
| S цилиндра = `π·d·h + π·d²/2`; подземное разделение `cap=π·d²/4` | `tank.py:98,128-129` | ✅ |
| Q_доп прибавляется после K; default `0.0` | `tank.py:306-307` | ✅ |
| λ трубы = `A + B·(T+40)` (carbon_steel a=60,b=−0.1) | `pipe_materials.json` | ✅ XLSX |

> Оговорка: совпадают **формулы и константы**. Независимый **численный** golden-расчёт
> по ним не прогонялся — независимого числового oracle теплопотерь среди
> первоисточников нет (см. §5).

## 1b. ✅ Подтверждено лично — электрика / справочники

| Факт | Доказательство |
|---|---|
| `_SERIES_LIMITS` ТТН 65/85, ТТВ 120/210, ТТХ 150/250 | `self_regulating.py:284-287` ✅ совпадает с ТНП |
| Выбор серии по T1 **и** T2 (пропарка) | `self_regulating.py:291-301` |
| Климат — **539** городов | `climate.json` |
| Теплоизоляция — **36** материалов, λ(tm) | `insulation.json` |
| Аксессуары — **6** generic | `accessories.json` |
| Резистивные: `single_core/three_core/common` | `resistive_cables.json` |
| СТ/СР после фикса: `"СР" if aggressive else "СТ"` | `self_regulating.py:318` 🔧 |

---

## 2. ❌ Опровергнуто (агент ошибался)

| Утверждение агента | Реальность (проверено лично) |
|---|---|
| λ изоляции «20 и выше» (warm `a+b·tm`) — **фабрикация** | **Неверно.** Формулы есть в `Теплоизоляция.docx` как OMML (26 шт., `m:oMath`); `python-docx .text` их не видит. `insulation.json`/`insulation.md` **корректны**. |
| Тестов backend **631** (299+331) | **Неверно.** Механически **947 unit + 350 integration = 1297** (`def test_`); CLAUDE.md был прав. |
| e2e тестов **92** | **Неверно.** **102** (генератор sync-docs). |
| `admin-formulas-sandbox` отсутствует в карте §10 манифеста | **Неверно.** Присутствует (строка 376). |

> Вывод: субагентские «на глаз» числа и проверки бинарных DOCX ненадёжны —
> перепроверять по сырому XML/механическому счёту.

---

## 3. ✅ Подтверждённые расхождения первоисточник ↔ код

| Расхождение | Доказательство | Серьёзность |
|---|---|---|
| **Каталог ТЛТ без первоисточника** — `ТЛТ-10…100` нет ни в одном PDF/DOCX/XLSX/VSDX (0 совпадений); «ТЛТ» = поставщик. Реальные марки = ТТН/ТТВ/ТТХ | `cables_tlt.json` vs поиск по всем бинарям | высокая |
| **Напряжение 220 vs 230** — qб-источник `I=P/230`, паспорт 230×6/220×0; код/JSON 220 | `cables_tt.json voltage=220`; `pipe-...md:125`, PDF | средняя |
| **Номиналы ТТН/ТТВ/ТТХ** — код ТТН 10/15/25/33, ТТХ …80/95 vs эталон ТТН 10/17/25/31, ТТХ 75/90 | `cables_tt.json` vs эталон r9-26 | средняя |
| **Спецификация** — 6 generic + плоский BOM vs условный BOM ~120 поз. | `accessories.json=6`, `builder.py` | высокая |
| **Полный перечень продукции** — 669 в первоисточнике, 6 в коде | `Полный перечень...xlsx` | средняя |
| **Справочник толщин труб** — не загружен в backend | нет файла в `reference_data/` | средняя |
| **Навив на D=75/89/108** — код `<=` относит к нижней ветке, ТНП строгое `<` (не покрыто) | `calculation_service.py:2314-2322` vs `winding.md` | средняя |
| **Диапазоны бака** — `d≤30` (источник 3.0), `сторона≤100` (50), `λ_гр_min=0.5` (0.8) | `calculation.py:28-35,153,370-376` | низкая |
| **Граница класса `<` vs `≤`** — код `>` (включительно), VSDX строгое `<` | `self_regulating.py:294` vs VSDX | средняя (решение) |

## 3b. Совпадения справочников — статус перепроверки

| Пункт | Статус |
|---|---|
| q1/q2 `cables_tt.json` совпадают с qб-таблицей | ✅ проверено лично (10/33ТТН2, 30/15ТТВ2 — построчно совпали с `pipe-...md`) |
| λ грунта значения совпадают с XLSX | ✅ проверено лично (0.86/1.11/1.92/0.71/0.9/0.83/1.04 = колонка D) |
| Порядок резистив-алгоритма (220→380, петля→звезда, M++) совпадает с VSDX | ✅ закрыто — сверено лично с VSDX (см. §3c), совпадает кроме шага U (1 vs 5) |
| `λ_гр` fallback в коде `1.5` (не из таблицы) | ⚙️ агент, низкая |

---

## 3c. ✅ Алгоритмы VSDX — перепроверены лично (текст блок-схем извлечён из `.vsdx`)

| Алгоритм | Результат сверки с кодом |
|---|---|
| **Навив** (`алгоритм навив.vsdx`) | Kn: `D<57→1.0`, `=57→1.1`, `57<D<75→1.2`, `75<D<89→1.3`, `89<D<108→1.4`, `D>108→1.5`, иначе «Конец» (75/89/108 не покрыты). ✅ строгие `<`; код использует `<=` → **расхождение на 75/89/108** (подтв.) |
| **Самрег трубы** (`Алгоритм Самрег.трубы.vsdx`) | Серия по `T1<65/T2<85`, `<120/<210`, `<150/<250` (строгие `<` ✅, код `≤` — расхождение). `qб=q1·T3+q2` ✅. Марка `D&C&"2-"&F`, пример `30ТТВ2-СР` ✅. **СТ/СР: `R=1(агресс)→F="СР"`, `Нет→"СТ"` — двойное подтверждение фикса** (VSDX + эталон). |
| **Климат** (`Алгоритм климат.vsdx`) | ✅ **Реализован верно** в `calculation_service.py:1054-1072`: труба `D≥100 → K=1.1, T=t_0_92`; труба `D<100 → K=1.12, T=t_abs_min`; резервуар `→ K=1.1, T=t_0_92`. Точное совпадение с блок-схемой. |
| **Резистив** (`Алгоритм подбора резистив.vsdx`) | ✅ структура совпадает: сортировка `Q(i,1)` по убыванию, `U=220 N=2 M=1` → `U=380` → `N=3(звезда)` → `N=2;M=M+1`, `Imax=65А`, `p2<p3`, `p=p2·N·M`, `I=p2/U`. |

### Новые находки из алгоритмов (требуют внимания)
| Находка | Деталь | Статус |
|---|---|---|
| **Резистив: шаг снижения U** | VSDX `U=U-1` (по 1 В), код `voltage_step=5`. Эффект — только оптимальность подбора, не корректность (p2<p3, I≤65 соблюдены). | ⚠️ низкая; решить 1 vs 5 (`winding-thread-count-analysis.md §6`) |
| **Самрег: N без k_навива** | Разобрано детально (`winding-thread-count-analysis.md`): по умолчанию `k=1` → код **точно совпадает** с VSDX; при заданном навиве код физически корректен (баланс мощности `q_б·k·N ≥ q_required`, недогрев исключён, двойного учёта нет). | ✅ **НЕ баг**; уточнить лист 2 алгоритма для буквального подтверждения |
| **Самрег: только ТТН/ТТВ/ТТХ** | алгоритма ТЛТ нет (подтверждает «каталог ТЛТ без первоисточника») | ✅ подтв. |
| **Самрег: «См. следующий лист»** | page 2 в `.vsdx`/PDF отсутствует — алгоритм неполный в репо (как и аудио) | ❓ нужен лист 2 |
| **Резистив: p3 при t3 (темп. жилы)** | VSDX считает p3 при макс. темп. жилы; код использует холодное R20 без `[1+0.0042(T_ж−20)]` | ⚠️ известный issue (задокументирован) |

> ⚙️-пункт «порядок резистив-алгоритма» из §3b — **закрыт**: сверено лично с VSDX,
> совпадает (кроме шага U).

---

## 4. 🔧 Исправлено в этой сессии

| Что | Где |
|---|---|
| Инверсия суффикса СТ/СР (агрессивная → теперь `-СР`) | `self_regulating.py:318` + docstring + `formulas-summary.md` + тесты; 570 тестов зелёные |
| `КСН-3/КСН-4` → `КСВ-1/КСВ-2` (высокотемп. заделки) | `spec-bom-oracle.md`, `heating-sections-...md §6` |
| Манифест: §6.4 mutmut, §11 drift-guard, числа в AUTO-блоке | `e2e/E2E-MANIFEST.md`, `scripts/sync-docs.py` |

---

## 5. ❓ Не знаем / нужны данные (блокеры спецификации)

| Неизвестное | Почему важно | Откуда взять |
|---|---|---|
| **`L_max`** — макс. длина нагревательной секции по маркам | без неё нет числа секций → нет заделок/коробок | «таблица Виктора» от заказчика |
| **`k_пуск`** — кратность пускового тока по маркам | пусковой ток секции (требование UI) | заказчик |
| **Методика MI** (минеральная изоляция) | ветка T1>150/T2>250 | заказчик |
| **Числовой oracle теплопотерь** | нет независимого эталона; `Мой проект.xlsx` = самоэкспорт приложения; есть риск циркулярности тестов | нужен ручной поверочный расчёт инженера |
| **Полная аудиозапись** | `Спека аудио.txt` обрывается (коробки/MI/резистив не дописаны) | заказчик |

### Открытые решения (не данные — выбор)
- ТЛТ: даташит или признать `cables_tlt.json` placeholder.
- Напряжение самрега 230 vs 220.
- Номиналы ТТН/ТТВ/ТТХ — какой каталог канон.
- Граница класса `<` vs `≤` (аудио исключено как основание).

---

## 5b. ТНП/ — пройдено полностью (2026-06-08)

**Инвентаризация.** Все файлы `Мой проект*` (`.xlsx/.pdf/.docx/.tlt-*.csv`) —
**самоэкспорты приложения** одного и того же демо-проекта (100 объектов: 60 труб +
40 баков). Это **не** независимый oracle (подтверждают сами себя). Усиливает вывод
§5: независимого числового эталона теплопотерь нет.

**Структура отчёта** (из `Мой проект-4.pdf`, 13 стр.): сводка (объекты, Σтеплопотери
593.16 кВт, Σмощность 529.88 кВт, длина 14225.7 м, Σток 2408.56 А) → таблица труб →
таблица баков (Форма/Габариты/S/q) → электрика (Объект, Марка, Длина, Уд.мощность,
Σмощность, Ток, U) → Спецификация. Полезно как референс состава отчёта.

**🔴 Живое доказательство трёх находок в самом выводе приложения:**
- Электрика: марки **ТЛТ-10/60/…**, **U=220 В** → подтверждает синтетический каталог
  ТЛТ и 220 (не 230).
- Спецификация (стр.13) = **ТЛТ-N кабели + 6 generic** (УЗО-30, КЗ-01, КС-01,
  ЛКА-50, ДТ-NTC, ТР-220). **Нет** ТТН/ТТВ/ТТХ, **нет** КСН/КСВ/СКВ/условного BOM →
  прямое подтверждение разрыва спецификации (§3).

**Прочее в ТНП/:**
- `Приложение_1_..._ТЗ.pdf` и `Договор...pdf` — **AES-зашифрованы** (pypdf не читает);
  содержание ТЗ доступно через корневой `ТЗ программа Web.pdf` (идентичен, разобран
  агентом ранее).
- `Блок теплопотери/*.docx,*.xlsx` и `Внутренние справочники/Климат.xlsx` — **дубли**
  уже разобранных (версии в `ТНП/` старее: tank docx без `Q_доп`); расхождений
  формул нет.

---

## 6. Что НЕ требует запроса (есть и сверено)
Климат 539, теплоизоляция 36 (λ(tm) из OMML), λ трубы, формулы теплопотерь,
`_SERIES_LIMITS`, формулы BOM + артикулы + 12 коробок СКВ + условия (Ex/К1i/К2i/Кiu/
L,К2i) + `R,гр` как user-input — всё в эталоне
`Расчет_спецификации_трубы_самрег29_05_26.xlsx` и справочниках.
