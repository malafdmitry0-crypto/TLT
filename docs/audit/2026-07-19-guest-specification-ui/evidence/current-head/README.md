# Current HEAD evidence

Baseline: `38f6bb3e44393e46a8b8a2ba88dfd622dccba33c`.

Key chain:

1. heat result: `q=15.0 W/m`, `Q=165 W`;
2. ER1: ТЛТ-20, installed 10.0 m, final order 11.0 m, 200 W, 0.91 A;
3. first specification POST: expected 409 with two excluded groups;
4. modal incorrectly says `Всего исключений: 0`; screenshot retained;
5. confirmation;
6. second POST: 201, six items, `partial=true`;
7. UI header/banner shows partial and both codes;
8. reload preserves the same status;
9. report preview contains partial diagnostics;
10. print button invokes `window.print()`.

Interactive narrow width is outside PDL-ER-30 Phase-5 scope; its evidence is
retained to assess warning/readability and print risk, not to redefine desktop
acceptance.
