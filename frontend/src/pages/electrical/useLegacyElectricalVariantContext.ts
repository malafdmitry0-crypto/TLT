/** Compatibility re-export — prefer `@/hooks/useLegacyElectricalVariantContext` (layout-safe). */
export {
  useLegacyElectricalVariantContext,
  default,
} from '@/hooks/useLegacyElectricalVariantContext';
