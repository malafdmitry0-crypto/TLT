/**
 * Stable public entry for App-bound feedback APIs.
 *
 * Prefer: `import { appMessage, appModal } from '@/feedback/appFeedback'`
 * Shell component: `import { AntdAppShell } from '@/feedback/AntdAppShell'`
 */
export {
  appMessage,
  appModal,
  appNotification,
} from './appFeedbackApi';
